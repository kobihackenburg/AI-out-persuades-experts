# GitHub Linguist configuration.
#
# Everything under output/ (figures, LaTeX tables, the numbers lookup) is
# generated by `make all`, not hand-written source. Mark it as generated so it
# is excluded from the repository's language statistics and collapsed in diffs.
output/** linguist-generated=true
